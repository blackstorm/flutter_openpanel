name: openpanel_flutter
description: >-
  Unofficial Flutter SDK for Openpanel (open-source Mixpanel alternative).
  Maintained fork: https://github.com/blackstorm/flutter_openpanel
version: 0.4.0
homepage: https://github.com/blackstorm/flutter_openpanel
topics:
  - analytics

environment:
  sdk: ^3.10.0
  flutter: ">=3.38.1"

dependencies:
  flutter:
    sdk: flutter
  device_info_plus: ^13.2.0
  http: ^1.6.0
  package_info_plus: ^10.2.1
  referrer: ^1.1.0
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
